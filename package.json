{
  "name": "annalex_estate",
  "scripts": {
    "ng": "ng",
    "start": "concurrently --kill-others \"go run main.go\" \"cd client && ng serve --open\"",
    "build": "cd client && ng build --prod"
  },
  "private": true,
  "devDependencies": {
    "@angular/cli": "~11.2.2",
    "@angular/compiler-cli": "~11.2.2",
    "concurrently": "^5.2.0"
  }
}
